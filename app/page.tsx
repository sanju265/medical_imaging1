import HomePageClient from "./HomePageClient"

export const metadata = {
  title: "SkinSight+ — Home",
  description: "Analyze dermoscopic images with AI to get predictions, probabilities, and severity insights.",
}

export default function HomePage() {
  return <HomePageClient />
}
